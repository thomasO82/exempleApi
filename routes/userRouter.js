const userModel = require("../models/userModel") //importation de notre modele d'utilisateur
const userRouter = require("express").Router() //importation et execution de la methode express Router() c'est grace a celle ci que nous pourront definir nos routes par la suite


//nos routes ici tout a l'heure


module.exports = userRouter //exportation de notre userRouter 